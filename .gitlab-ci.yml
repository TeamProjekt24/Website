image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r * public/ 2>/dev/null || true
    - cp -r .[^.]* public/ 2>/dev/null || true
  artifacts:
    paths:
      - public
  only:
    - main
    - master

